<%= form_with(model: topicss, local: true) do |form| %>
  <% if topicss.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topicss.errors.count, "error") %> prohibited this topicss from being saved:</h2>

      <ul>
        <% topicss.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
